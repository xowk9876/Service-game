# Tae_system Portfolio Website

👨‍💻 **웹 개발자 & 포트폴리오 전문가** - 현대적이고 매력적인 개인 포트폴리오 웹사이트

## 🌟 프로젝트 개요

Tae_system의 개인 포트폴리오 웹사이트로, 웹 개발자 & IT 서버 엔지니어로서의 전문성과 창의성을 보여주는 현대적인 디자인의 반응형 웹사이트입니다. 글래스모피즘 디자인, 다크 모드, 스크롤 애니메이션, **실시간 익명 채팅 시스템** 등 최신 웹 트렌드를 반영한 인터랙티브한 사용자 경험을 제공합니다.

### 🚀 **주요 기능 (v3.1.0) - 배포 완료**
- **실시간 익명 채팅**: WebSocket 기반 다중 사용자 실시간 채팅 ✅
- **완전 익명 시스템**: 개인정보 없이 안전한 채팅 환경 ✅
- **실시간 동기화**: 메시지, 타이핑 상태, 사용자 수 실시간 업데이트 ✅
- **자동 재연결**: 연결 끊김 시 자동 재연결 기능 ✅
- **반응형 디자인**: 모바일/데스크톱 모든 기기 지원 ✅
- **전 세계 접속**: 어디서나 실시간 채팅 가능 ✅

## ✨ 주요 기능

### 🎯 프로필 섹션
- **개인 정보**: 웹 개발자 & IT 서버 엔지니어 소개
- **연락처**: 이메일, 위치 정보 및 소셜 미디어 링크
- **액션 버튼**: 이력서 다운로드, 익명 채팅하기 기능
- **다크 모드**: 우측 상단 토글 버튼으로 테마 변경

### 🚀 전문 서비스
- **웹사이트 성능 최적화**: 로딩 속도 3배 향상 솔루션
- **개인/기업 포트폴리오 제작**: 브랜드 스토리를 담은 아름다운 웹사이트
- **반응형 웹 개발**: 모든 기기에서 완벽하게 작동하는 웹사이트
- **웹사이트 유지보수**: 24/7 관리 서비스
- **서버 인프라 구축**: Linux, Windows Server, Docker, Kubernetes
- **클라우드 솔루션**: AWS, Azure 기반 확장 가능한 아키텍처
- **CI/CD 파이프라인**: 자동화된 배포 및 모니터링 시스템

### 🌟 프로젝트
- **실시간 익명 채팅 시스템**: WebSocket 기반 다중 사용자 실시간 채팅 플랫폼 ✅
  - **배포 상태**: 정상 작동 중
  - **접속 URL**: [https://xowk9876.github.io/portfolio/chat.html](https://xowk9876.github.io/portfolio/chat.html)
  - **기술 스택**: Node.js, WebSocket, GitHub Pages, Render
- **EmuOS 레트로 게임 플랫폼**: 10만+ 사용자, 1000+ 게임, 4.8/5 평점
- **포트폴리오 웹사이트 템플릿**: 반응형, 다크모드, 빠른 속도
- **실시간 대시보드 시스템**: 실시간 모니터링 및 알림 기능 (비공개)
- **서버 자동화 시스템**: Ansible 기반 90% 시간 단축 솔루션 (비공개)
- **보안 모니터링 시스템**: ELK Stack 기반 실시간 보안 이벤트 모니터링 (비공개)

### 💻 전문 기술
- **프론트엔드**: HTML5, CSS3, JavaScript (ES6+), React, Vue.js
- **백엔드**: Node.js, Python, Java, C#
- **실시간 통신**: WebSocket, Socket.IO, Server-Sent Events
- **데이터베이스**: MySQL, PostgreSQL, MongoDB, Redis
- **서버 & 인프라**: Linux, Windows Server, Docker, Kubernetes
- **클라우드**: AWS, Azure, Google Cloud Platform
- **모니터링**: ELK Stack, Grafana, Prometheus
- **자동화**: Ansible, Jenkins, GitLab CI/CD

### 📚 최신 기술 글
- **웹사이트 로딩 속도 3배 빠르게 만드는 방법** (리다이렉트 준비중)
- **포트폴리오 웹사이트 디자인 트렌드 2025** (리다이렉트 준비중)
- **반응형 웹 디자인 완벽 가이드** (리다이렉트 준비중)
- **Linux 서버 보안 강화 완벽 가이드** (리다이렉트 준비중)
- **Ansible을 활용한 서버 자동화** (리다이렉트 준비중)

## 🎨 디자인 특징

### 글래스모피즘 & 현대적 UI
- **반투명 유리 효과**: backdrop-filter를 활용한 현대적인 디자인
- **그라데이션 애니메이션**: 다채로운 배경 색상 변화
- **부드러운 애니메이션**: CSS3 기반의 자연스러운 전환 효과
- **스크롤 애니메이션**: 섹션별 페이드인, 슬라이드 효과

### 다크 모드 지원
- **테마 토글**: 우측 상단 버튼으로 라이트/다크 모드 전환
- **로컬 저장**: 사용자 테마 선택 기억
- **일관된 디자인**: 모든 섹션에서 통일된 다크 모드 스타일

### 반응형 디자인
- **모바일 최적화**: 320px부터 완벽한 모바일 경험
- **태블릿 지원**: 768px-1024px 중간 화면 최적화
- **데스크톱**: 1920px 이상 대형 화면 지원

## 🚀 기술 스택

### Frontend
- **HTML5**: 시맨틱 마크업과 접근성 고려
- **CSS3**: Flexbox, Grid, 애니메이션, 글래스모피즘
- **JavaScript (ES6+)**: 모던 JavaScript 기능 활용
- **Font Awesome**: 아이콘 라이브러리
- **반응형 디자인**: 모바일 퍼스트 접근법

### Backend & 실시간 통신
- **Node.js**: WebSocket 서버
- **WebSocket**: 실시간 양방향 통신
- **비동기 처리**: async/await, Promise 기반
- **로컬 스토리지**: 사용자 설정 및 상태 관리

### 성능 최적화
- **Google Fonts**: preconnect로 폰트 로딩 최적화
- **이미지 최적화**: WebP 형식 및 적절한 크기 조정
- **코드 분할**: 필요한 기능만 로드
- **캐싱**: 정적 자원 캐싱 전략
- **메시지 최적화**: 오래된 메시지 자동 제거

### SEO 최적화
- **메타 태그**: title, description, keywords 최적화
- **Open Graph**: 소셜 미디어 공유 최적화
- **Twitter Card**: 트위터 공유 최적화
- **구조화된 데이터**: JSON-LD 스키마 적용
- **사이트맵**: robots.txt, sitemap.xml 제공
- **다중 페이지**: index, community, chat 페이지 지원

## 📁 파일 구조

```
WebSite/
├── index.html              # 메인 포트폴리오 페이지
├── community.html          # 익명 커뮤니티 포털
├── chat.html               # 실시간 익명 채팅 시스템
├── styles.css              # 통합 스타일시트
├── script.js               # JavaScript 기능
├── chat-server.js          # WebSocket 서버 (Node.js)
├── package.json            # Node.js 프로젝트 설정
├── README.md               # 프로젝트 문서
├── robots.txt              # 검색 엔진 크롤링 정책
├── sitemap.xml             # 사이트맵
├── LICENSE                 # 라이선스
└── image/
    └── profile.jpg         # 프로필 사진
```

## 🚀 사용법

### 📱 **기본 사용법**
1. **메인 페이지**: 포트폴리오 및 서비스 소개 확인
2. **채팅하기 버튼**: 익명 커뮤니티 포털로 이동
3. **실시간 채팅**: 익명으로 다른 사용자들과 실시간 대화
4. **다크 모드**: 우측 상단 토글 버튼으로 테마 변경

### 💬 **실시간 익명 채팅 사용법**
1. **채팅하기** 버튼 클릭
2. **익명 채팅** 페이지로 이동
3. **닉네임 설정** (자동 생성되거나 수동 입력)
4. **메시지 입력** 후 전송
5. **다른 사용자들과 실시간 대화** 가능
6. **명령어 사용**:
   - `/nick 새닉네임`: 닉네임 변경
   - `/join 채널명`: 채널 참여
   - `/help`: 도움말 보기

## 🌐 배포

### GitHub Pages 배포 ✅
- **배포 URL**: `https://xowk9876.github.io/portfolio/`
- **채팅 URL**: `https://xowk9876.github.io/portfolio/chat.html`
- **배포 방식**: GitHub Pages (main 브랜치, root 폴더)
- **자동 배포**: 코드 변경 시 자동으로 사이트 업데이트
- **HTTPS 지원**: 무료 SSL 인증서 제공

### 실시간 채팅 시스템 배포 ✅
- **프론트엔드**: GitHub Pages (정적 파일)
- **백엔드**: Render WebSocket 서버 호스팅 완료
- **WebSocket 서버**: `https://portfolio-q8ar.onrender.com`
- **채팅 URL**: `https://xowk9876.github.io/portfolio/chat.html`
- **상태**: 실시간 채팅 정상 작동 중

## 💻 코드 예시

### **WebSocket 서버 구현**
```javascript
// chat-server.js
const WebSocket = require('ws');
const wss = new WebSocket.Server({ port: 8080 });

const clients = new Map();
const messageHistory = [];

wss.on('connection', (ws) => {
    const clientId = generateId();
    clients.set(ws, { id: clientId, nickname: `사용자${clientId}` });
    
    ws.on('message', (data) => {
        const message = JSON.parse(data);
        handleMessage(ws, message);
    });
    
    ws.on('close', () => {
        clients.delete(ws);
        broadcastUserCount();
    });
});

function handleMessage(ws, message) {
    const client = clients.get(ws);
    
    switch (message.type) {
        case 'nickname':
            client.nickname = message.nickname;
            broadcastUserCount();
            break;
        case 'message':
            broadcastMessage(client.nickname, message.content);
            break;
        case 'typing':
            broadcastTyping(client.nickname, true);
            break;
        case 'stopTyping':
            broadcastTyping(client.nickname, false);
            break;
    }
}
```

### **클라이언트 WebSocket 연결**
```javascript
// chat.html
function connectWebSocket() {
    const wsUrl = 'wss://portfolio-q8ar.onrender.com';
    
    try {
        ws = new WebSocket(wsUrl);
        
        ws.onopen = function() {
            console.log('WebSocket 연결 성공');
            isConnected = true;
            addSystemMessage('🔗 실시간 채팅 서버에 연결되었습니다!');
            
            if (currentNickname) {
                ws.send(JSON.stringify({
                    type: 'nickname',
                    nickname: currentNickname
                }));
            }
        };
        
        ws.onmessage = function(event) {
            const data = JSON.parse(event.data);
            handleWebSocketMessage(data);
        };
        
        ws.onclose = function() {
            console.log('WebSocket 연결 종료');
            isConnected = false;
            addSystemMessage('❌ 서버 연결이 끊어졌습니다. 재연결을 시도합니다...');
            setTimeout(connectWebSocket, 3000);
        };
        
        ws.onerror = function(error) {
            console.error('WebSocket 오류:', error);
            addSystemMessage('❌ 연결 오류가 발생했습니다.');
        };
    } catch (error) {
        console.error('WebSocket 연결 실패:', error);
        addSystemMessage('❌ 실시간 채팅을 사용할 수 없습니다.');
    }
}
```

### **메시지 전송 및 표시**
```javascript
// 메시지 전송
function sendMessage() {
    const message = messageInput.value.trim();
    const nickname = nicknameInput.value.trim() || currentNickname;
    
    if (!message) return;
    
    if (isConnected && ws) {
        ws.send(JSON.stringify({
            type: 'message',
            content: message
        }));
    } else {
        addMessage(nickname, message);
    }
    
    messageInput.value = '';
    messageInput.focus();
}

// 메시지 표시
function displayMessage(nickname, message, isOwn = false) {
    const messageElement = document.createElement('div');
    messageElement.className = `message ${isOwn ? 'own' : ''}`;
    
    messageElement.innerHTML = `
        <div class="message-header">
            <span class="nickname">${nickname}</span>
            <span class="timestamp">${new Date().toLocaleTimeString()}</span>
        </div>
        <div class="message-content">${message}</div>
    `;
    
    messagesContainer.appendChild(messageElement);
    messagesContainer.scrollTop = messagesContainer.scrollHeight;
}
```

### **다크 모드 구현**
```javascript
// script.js
function toggleDarkMode() {
    const body = document.body;
    const isDark = body.classList.toggle('dark-mode');
    
    localStorage.setItem('darkMode', isDark);
    updateDarkModeButton(isDark);
}

function updateDarkModeButton(isDark) {
    const darkModeBtn = document.getElementById('darkModeBtn');
    if (darkModeBtn) {
        darkModeBtn.innerHTML = isDark ? '☀️' : '🌙';
        darkModeBtn.title = isDark ? '라이트 모드로 변경' : '다크 모드로 변경';
    }
}

// 페이지 로드 시 다크 모드 상태 복원
document.addEventListener('DOMContentLoaded', function() {
    const savedDarkMode = localStorage.getItem('darkMode') === 'true';
    if (savedDarkMode) {
        document.body.classList.add('dark-mode');
        updateDarkModeButton(true);
    }
});
```

### **반응형 CSS 그리드**
```css
/* styles.css */
.blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 30px;
    margin-top: 30px;
    align-items: start;
}

.blog-card {
    background: white;
    border-radius: 20px;
    padding: 0;
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    position: relative;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.2);
    display: flex;
    flex-direction: column;
    height: 100%;
}

@media (max-width: 768px) {
    .blog-grid {
        grid-template-columns: 1fr;
        gap: 25px;
    }
}
```

## 🔧 개발 환경

### 로컬 개발
1. **Python 웹서버 실행**:
   ```bash
   python -m http.server 8000
   ```

2. **Node.js WebSocket 서버 실행**:
   ```bash
   npm install
   node chat-server.js
   ```

3. **브라우저에서 접속**:
   - `http://localhost:8000`
   - `http://127.0.0.1:8000`

### 프로덕션 환경
- **CDN**: 전 세계 빠른 접속 속도
- **무료 호스팅**: 비용 없이 안정적인 서비스
- **온라인 호환성**: 모든 브라우저에서 완벽 작동

## 🎯 성능 최적화

### JavaScript 최적화
- **이벤트 위임**: 메모리 효율적인 이벤트 처리
- **애니메이션 프레임**: 부드러운 애니메이션 처리
- **디바운싱**: 버튼 클릭 중복 방지
- **로컬 스토리지**: 사용자 설정 저장

### CSS 최적화
- **GPU 가속**: transform, opacity 애니메이션 활용
- **CSS 변수**: 일관된 색상 및 크기 관리
- **미디어 쿼리**: 반응형 디자인 최적화
- **SVG 아이콘**: 벡터 기반 아이콘으로 선명한 표시

## 🔍 SEO 구성 요소

### 메타 태그
- **Title**: "Tae_system - 웹 개발자 & 포트폴리오 전문가 | 반응형 웹사이트 제작"
- **Description**: 핵심 서비스와 전문성을 포함한 설명
- **Keywords**: 웹개발자, 포트폴리오, 반응형웹사이트 등 관련 키워드
- **Open Graph**: 소셜 미디어 공유 최적화
- **Twitter Card**: 트위터 공유 최적화

### 구조화된 데이터
- **JSON-LD**: Person 스키마로 작성자 정보 구조화
- **knowsAbout**: 전문 기술 영역 명시
- **hasOccupation**: 직업 정보 구조화

### 검색 엔진 최적화
- **robots.txt**: 크롤링 정책 설정
- **sitemap.xml**: 사이트 구조 제공
- **Canonical URL**: 중복 콘텐츠 방지

## 🔧 최신 업데이트

### v3.1.0 - 2025.09.07 (최신)
- **🌐 도메인 업데이트**: `https://xowk9876.github.io/portfolio/`로 전체 URL 변경
- **🔒 프로젝트 상태 업데이트**: 서버 자동화 시스템, 보안 모니터링 시스템을 비공개로 변경
- **📝 기술 글 상태**: 모든 기술 글을 "리다이렉트 준비중"으로 업데이트
- **🔍 SEO 최적화**: 모든 메타 태그, sitemap, robots.txt URL 업데이트
- **💬 실시간 채팅**: WebSocket 기반 다중 사용자 실시간 채팅 시스템 완성
- **🎨 UI/UX 개선**: 블로그 카드 레이아웃 최적화 및 간격 정렬

### v3.0.0 - 2025.09.07
- **🎉 익명 커뮤니티 포털**: 완전한 익명 채팅 시스템 구현
- **💼 서버 엔지니어 전문성**: IT 인프라 및 클라우드 기술 통합
- **📱 완전 반응형**: 모바일, 태블릿, 데스크톱 완벽 지원
- **🌙 다크 모드**: 모든 페이지에서 일관된 다크 모드 지원

### v2.0.0 - 2025.09.07
- **서비스 섹션 개선**: 현실적이고 매력적인 서비스 내용으로 업데이트
- **프로젝트 쇼케이스 강화**: 통계와 이모지를 활용한 눈에 띄는 프로젝트 소개
- **기술 스킬 현대화**: 웹 개발 중심의 실용적인 기술 스택 정리
- **블로그 섹션 추가**: 최신 기술 글과 트렌드 소개
- **다크 모드 구현**: 사용자 경험 향상을 위한 테마 토글 기능
- **스크롤 애니메이션**: 섹션별 부드러운 등장 효과
- **SEO 최적화**: 검색 엔진 최적화 및 소셜 미디어 공유 개선

### v1.0.0 - 2025.09.07
- **기본 포트폴리오 구조**: 프로필, 서비스, 프로젝트 섹션
- **반응형 디자인**: 모바일, 태블릿, 데스크톱 지원
- **글래스모피즘 디자인**: 현대적인 UI/UX
- **GitHub Pages 배포**: 무료 호스팅 및 자동 배포

## 📞 연락처

- **이메일**: bhd03014@gmail.com
- **인스타그램**: [@tae_system](https://www.instagram.com/tae_system/)
- **GitHub**: [@xowk9876](https://github.com/xowk9876)
- **위치**: South Korea
- **전문 분야**: 웹 개발, IT 서버 엔지니어링, 실시간 통신 시스템

## 🎯 주요 성과

- **포트폴리오 웹사이트**: 현대적이고 반응형 디자인
- **실시간 채팅 시스템**: WebSocket 기반 다중 사용자 실시간 채팅
- **완전 반응형**: 모든 기기에서 완벽한 사용자 경험
- **클라우드 배포**: GitHub Pages + Render를 통한 안정적인 서비스

## 🔧 문제 해결

### **자주 묻는 질문 (FAQ)**

#### **Q: 실시간 채팅이 작동하지 않아요**
```bash
# 해결 방법:
1. 인터넷 연결 상태 확인
2. 브라우저 콘솔에서 오류 메시지 확인
3. WebSocket 서버 상태 확인
4. 방화벽 설정 확인
```

#### **Q: 닉네임이 제대로 표시되지 않아요**
```bash
# 해결 방법:
1. 페이지 새로고침 후 재시도
2. 다른 닉네임으로 변경 시도
3. 브라우저 캐시 삭제
```

#### **Q: 모바일에서 레이아웃이 깨져요**
```bash
# 해결 방법:
1. 브라우저 최신 버전 사용
2. 페이지 새로고침
3. 브라우저 캐시 삭제
```

### **기술 지원**
- **이메일**: bhd03014@gmail.com
- **GitHub Issues**: [프로젝트 저장소](https://github.com/xowk9876/portfolio)
- **문서**: README.md 참조

## 📄 라이선스

이 프로젝트는 개인 포트폴리오 목적으로 제작되었습니다.

---

**Tae_system** - 웹 개발자 & IT 서버 엔지니어  
*✨ 최신 업데이트: v3.1.0 - 실시간 채팅 시스템 완성*  
*🌐 온라인 호환성: 모든 브라우저와 기기에서 완벽 작동*  
*💬 실시간 채팅: WebSocket 기반 다중 사용자 실시간 대화 가능*